DATABASE_URL="${{Postgres.DATABASE_URL}}"
DB_BACKUP_CRON="0 2 * * *"
DB_BACKUP_ENABLED="true"
DB_BACKUP_PREFIX="pos-system-backup"
DB_BACKUP_TZ="Asia/Tashkent"
ESKIZ_BASE_URL="https://notify.eskiz.uz"
ESKIZ_EMAIL="ulugbekmirdadayev1211@gmail.com"
ESKIZ_FROM="4546"
ESKIZ_PASSWORD="Bw7DmnPfLExLF618Am0C5MZetVKGGIcj0mFnnd9x"
JWT_EXPIRE="30d"
JWT_SECRET="hadya-skladprojectjwtsecret"
NODE_ENV="production"
PGDATABASE="${{Postgres.PGDATABASE}}"
PGHOST="${{Postgres.PGHOST}}"
PGPASSWORD="${{Postgres.PGPASSWORD}}"
PGPORT="${{Postgres.PGPORT}}"
PGUSER="${{Postgres.PGUSER}}"
PORT="8080"
S3_ACCESS_KEY_ID="${{pos-system-storage.ACCESS_KEY_ID}}"
S3_BUCKET="${{pos-system-storage.BUCKET}}"
S3_ENDPOINT="${{pos-system-storage.ENDPOINT}}"
S3_FORCE_PATH_STYLE="true"
S3_PUBLIC_URL="${{pos-system-storage.ENDPOINT}}"
S3_REGION="us-west-2"
S3_SECRET_ACCESS_KEY="${{pos-system-storage.SECRET_ACCESS_KEY}}"
# SMS o'chirgich: "false" — hech qanday SMS jo'natilmaydi (Eskiz almashtirilayotgan paytda).
# Eskiz tayyor bo'lganda "true" qiling yoki bu qatorni o'chiring.
SMS_ENABLED="false"